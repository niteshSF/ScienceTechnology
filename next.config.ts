import type { NextConfig } from "next"

const nextConfig: NextConfig = {
  images: {
    // domains: ["localhost", "test3.samskritifoundation.org"],
    domains: ['api.mdr.samskritifoundation.org'],
  },
}

export default nextConfig
